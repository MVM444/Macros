def add(a: float = 2.0, b: float = 3.0) -> float:
    """Suma simple para probar carga de nodos."""
    return float(a) + float(b)
def multiply(a: float = 2.0, b: float = 3.0) -> float:
    """Multiplicación simple para probar carga de nodos."""
    return float(a) * float(b)